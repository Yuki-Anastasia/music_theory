import { defineConfig, devices } from "@playwright/test";

/**
 * Analysis (score parsing, Basic Pitch, all scoring math) runs entirely
 * client-side and can be slow on a cold model load — generous per-test
 * timeouts here are deliberate, not a smell.
 */
export default defineConfig({
  testDir: "./e2e",
  fullyParallel: false,
  workers: 1,
  timeout: 60_000,
  expect: { timeout: 10_000 },
  reporter: "list",
  use: {
    baseURL: "http://localhost:3000",
    trace: "retain-on-failure",
  },
  projects: [{ name: "chromium", use: { ...devices["Desktop Chrome"] } }],
  webServer: {
    command: "npm run dev",
    url: "http://localhost:3000",
    reuseExistingServer: !process.env.CI,
    timeout: 60_000,
  },
});
